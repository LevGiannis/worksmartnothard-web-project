# Manager — Λογική Υπολογισμού ανά Κατηγορία

---

## Αναγνώριση αρχείου

Το σύστημα ανιχνεύει αυτόματα την κατηγορία κάθε Excel βάσει των headers:

| Header που βρίσκει   | Κατηγορία      |
| -------------------- | -------------- |
| `Ημ/νία Αίτησης`     | Mobile         |
| `MSISDN`             | Prepay         |
| `Τηλέφωνο Υπηρεσίας` | Vodafone Home  |

---

## Mobile

### Ποιες εγγραφές μετράνε

Μόνο τα παρακάτω subcategories (`Τύπος Αίτησης`) υπολογίζονται:

- EX PREPAY, PRE2EC, PORT IN POSTPAY, PORT IN EC, NEW CONNECTION, NEW EC, PREPAY 2 EC

Εξαιρούνται πάντα: εγγραφές με `TRANSFER` στον τύπο, και εγγραφές με status `ΑΚΥΡΩΜΕΝΗ`.

### Καταχωρημένες μήνα

Χρησιμοποιεί: **`Ημ/νία Αίτησης`** (date)

> Η αίτηση ανήκει στον μήνα που υποβλήθηκε.

### Συνδεδεμένες μήνα

Χρησιμοποιεί: **`Ημερομηνία Έγκρισης`** (implDate) — αν είναι κενή, fallback στο `Ημ/νία Αίτησης`  
Status απαιτείται: **`ΟΛΟΚΛΗΡΩΘΗΚΕ`**

### Ειδική περίπτωση: PORT IN PREPAY

Εγγραφές mobile με subcategory `PORT IN PREPAY` μετράνε στη στήλη **Prepay** (όχι Mobile).  
Χρησιμοποιεί implDate ή date, status `ΟΛΟΚΛΗΡΩΘΗΚΕ`.

### Πεδίο Connections

Αν το πεδίο `Αριθμός Συνδέσεων` > 1, χρησιμοποιείται ως πολλαπλασιαστής στο σύνολο.

### Μηνιαίο panel: Mobile / Προέγκριση / Prepay

Ακριβώς πάνω από τα δύο παράθυρα Vodafone Home, τρία panels δίπλα-δίπλα:

- **Mobile — Συνδεδεμένα Μήνα**: όλες οι mobile εγγραφές που ολοκληρώθηκαν τον μήνα, ομαδοποιημένες ανά subcategory (`classifyMobileSubcat` — Ex Prepay, Port In Postpay/EC, New Connection/EC, Pre2EC, Prepay 2 EC), ταξινομημένες κατά πλήθος.
- **Mobile — Προέγκριση**: σύνολο mobile αιτήσεων με status `ΠΡΟΕΓΚΡΙΣΗ` (ίδιο σύνολο με `mobilePending`, χωρίς μηνιαίο φίλτρο), με breakdown ανά subcategory (`classifyMobileSubcat`, ίδιο ταξινόμηση με το "Συνδεδεμένα Μήνα" — Ex Prepay, Port In Postpay/EC, New Connection/EC, Pre2EC, Prepay 2 EC).
- **Prepay — Συνδεδεμένα Μήνα**: όλες οι prepay εγγραφές που ολοκληρώθηκαν τον μήνα, περιλαμβανομένων των Port In Prepay (reclassified από το mobile αρχείο).

### Sparkline προόδου + ρυθμός στόχου (`PaceRow`)

Σε τρία panels υπάρχει μικρό sparkline δίπλα στον μεγάλο αριθμό: Mobile — Συνδεδεμένα Μήνα, Prepay — Συνδεδεμένα Μήνα, και **Vodafone Home — Μετράνε στον Μήνα** (Window B, όχι το Window A "Συνδεδεμένα Μήνα" — ο ρυθμός/στόχος έχει νόημα μόνο πάνω στο επίσημο KPI που μετράει, όχι στο ακατέργαστο "ό,τι συνδέθηκε ανεξαρτήτως καταχώρησης"):

- **Πραγματική γραμμή** (`buildDailyCumulative`): σωρευτικό πλήθος ανά ημέρα του μήνα — implDate με fallback στο date για Mobile/Prepay· για Home Window B, implDate με fallback στο date (ώστε οι FTTH "Υπό Υλοποίηση" entries, που δεν έχουν implDate, να εμφανίζονται στη μέρα καταχώρησής τους).
- **Γραμμή ρυθμού-στόχου** (διακεκομμένη): ευθεία από 0 στο `Στόχος` (νέο πεδίο input δίπλα στο sparkline, ανά κατηγορία/μήνα — ίδιο `monthlyTargets`/`getDoneTarget`/`setDoneTarget` που χρησιμοποιούσε το παλιό, αφαιρεμένο, scoreboard).
- **Απόκλιση ρυθμού** (`computePaceDelta`): διαφορά πραγματικού από τον αναμενόμενο ρυθμό στη "σημερινή" ημέρα (ή στο τέλος του μήνα, αν προβάλλεις παλιότερο μήνα) — πράσινο αν μπροστά, κόκκινο αν πίσω. Χωρίς Στόχο δεν εμφανίζεται απόκλιση, μόνο η καμπύλη.

### Απόδοση ομάδας — Καταχωρήσεις ανά Ημέρα (`DailyBarChart`)

Δύο νέα panels (Mobile, Vodafone Home) πάνω από τα δύο παράθυρα Vodafone Home, με ένα bar chart έντασης: μία μπάρα ανά ημέρα του μήνα, ύψος = πλήθος εγγραφών **της κατηγορίας που καταχωρήθηκαν** (`Ημ/νία Αίτησης` / `Ημ/νια Δημιουργίας Αίτησης`) εκείνη τη μέρα, αθροισμένο σε όλη την ομάδα του καταστήματος (`regMonthEntries` ανά κατηγορία, `buildDailyCounts`). Δείχνει τον ημερήσιο ρυθμό καταχωρήσεων — όχι συνδέσεις/ολοκληρώσεις. Ημέρες χωρίς καταχωρήσεις εμφανίζονται σαν αχνή μπάρα (opacity 0.15) ώστε να ξεχωρίζουν από κενό διάστημα. Native tooltip (hover) σε κάθε μπάρα δείχνει ακριβή ημέρα + πλήθος.

---

## Prepay

### Καταχωρημένες μήνα

Χρησιμοποιεί: **`Ημερομηνία Δημιουργίας`**

### Συνδεδεμένες μήνα

Χρησιμοποιεί: **`Ημερομηνία Ολοκλήρωσης`** — αν κενή, fallback στο `Ημερομηνία Δημιουργίας`  
Status απαιτείται: **`ΟΛΟΚΛΗΡΩΘΗΚΕ`**

---

## Vodafone Home

### Ποιες εγγραφές μετράνε

Όλες εκτός από εγγραφές με `TRANSFER` στο subcategory (`Προγραμμά Χρήσης · Ταχύτητα`).

### Ταξινόμηση προϊόντος (`classifyHomeProduct`)

Κάθε γραμμή Home ταξινομείται σε έναν από τέσσερις τύπους, με βάση το `subCategory` (= `Προγραμμά Χρήσης · Ταχύτητα`):

| Τύπος     | Κανόνας ανίχνευσης                                            |
| --------- | -------------------------------------------------------------- |
| One Net   | Ξεχωριστή γραμμή από το αρχείο **Mobile** με `Τύπος Αίτησης` που περιέχει `FIXED ACTIVATION` — αναταξινομείται αυτόματα σε Home |
| Wireless  | `Προγραμμά Χρήσης` περιέχει `Wireless`                          |
| FTTH      | `Ταχύτητα` περιέχει `FTTH`                                      |
| FTTC      | οτιδήποτε άλλο (fallback)                                       |

### Καταχωρημένες μήνα

Χρησιμοποιεί: **`Ημ/νια Δημιουργίας Αίτησης`**

### Δύο παράθυρα μηνιαίας ανάλυσης

**① Συνδεδεμένα Μήνα** — όλες οι γραμμές Home που συνδέθηκαν (Κ5) τον μήνα, **ανεξαρτήτως πότε καταχωρήθηκαν**, με ανάλυση ανά τύπο (FTTH / FTTC / Wireless / One Net).

- Χρησιμοποιεί: **`Ημ/νια Ολοκλήρωσης (Κ5)`** — **αυστηρά, χωρίς fallback**. Αν δεν υπάρχει Κ5 ημερομηνία, η εγγραφή δεν μετράει ως συνδεδεμένη.
- Status απαιτείται: `ΟΛΟΚΛΗΡΩΘΗΚΕ` ή `ΥΛΟΠΟΙΗΜΕΝΗ`

**② Μετράνε στον Μήνα** (επίσημο KPI/στόχος) — διαφορετικός κανόνας ανά τύπο:

- **FTTC / Wireless / One Net**: μετράνε όλα τα συνδεδεμένα του μήνα (ίδιο σύνολο με το παράθυρο ①), ανεξαρτήτως πότε καταχωρήθηκαν.
- **FTTH**: μετράει **μόνο** αν συνδέθηκε (Κ5) τον μήνα **ΚΑΙ** καταχωρήθηκε (`Ημ/νια Δημιουργίας Αίτησης`) τον **ίδιο** μήνα.
- **FTTH σε εκκρεμότητα**: επιπλέον μετράνε όσες FTTH καταχωρήθηκαν τον μήνα και έχουν status `ΥΠΟ ΥΛΟΠΟΙΗΣΗ` (δεν έχουν συνδεθεί ακόμα — "πίστωση" για το ότι μπήκαν στο pipeline μέσα στον μήνα).

> Μια FTTH που καταχωρήθηκε π.χ. τον Ιούλιο αλλά συνδέθηκε τον Αύγουστο **δεν** μετράει στο ② του Αυγούστου (ούτε στο ② του Ιουλίου, αφού δεν είχε συνδεθεί ακόμα) — εμφανίζεται μόνο στο παράθυρο ① του Αυγούστου ως ενημερωτικό στοιχείο.

---

## Panels πλάγιου πληροφόρησης

### Εκκρεμείς / Δικαιολογητικά

Εμφανίζει **μεμονωμένες αιτήσεις** (με customer, requestId, status) για:

| Κατηγορία | Status που εμφανίζεται                                                  | Εύρος ημερομηνιών                              |
| --------- | ----------------------------------------------------------------------- | ----------------------------------------------- |
| Home      | ΛΑΘΟΣ, ΕΛΛΙΠΗ ΔΙΚΑΙΟΛΟΓΗΤΙΚΑ, ΕΚΚΡΕΜΗ ΔΙΚΑΙΟΛΟΓΗΤΙΚΑ, ΚΑΤΑΧΩΡΗΜΕΝΗ, ΝΕΑ | Επιλέξιμο (date picker του panel, `docFromDate`) |
| Mobile    | ΝΕΑ, ΚΑΤΑΧΩΡΗΜΕΝΗ                                                       | Πάντα μόνο οι τελευταίες 10 ημέρες (`Ημ/νία Αίτησης`), ανεξάρτητα από το date picker |

Κάθε κάρτα πωλητή δείχνει, κάτω από το όνομα/σύνολο, breakdown των **δικών του** εγγραφών ανά ακριβές status (π.χ. "3 ΕΚΚΡΕΜΗ ΔΙΚΑΙΟΛΟΓΗΤΙΚΑ · 2 ΝΕΑ") — ώστε να φαίνεται αμέσως τι είδους πρόβλημα έχει ο καθένας, όχι μόνο πόσα.

### Υπό Υλοποίηση

Εμφανίζει **donut (κούφια πίτα) ανά πωλητή** — κάθε πωλητής η δική του φέτα (χωρίς fold σε "Λοιποί", όσοι πωλητές κι αν υπάρχουν), με πλήθος + ποσοστό δίπλα σε λίστα-legend, και το σύνολο γραμμένο στο κέντρο του δαχτυλιδιού. Μικρό γωνιακό κενό ανάμεσα στις φέτες (χωρίς stroke separator) για καθαρότερο διαχωρισμό. Χρώματα: οι πρώτες 8 φέτες παίρνουν το validated categorical palette· από την 9η και πάνω παράγονται επιπλέον αποχρώσεις (βήμα golden-angle στον κύκλο HSL) ώστε να μένουν οπτικά διακριτές. Κλικ σε φέτα ή σε γραμμή του legend ανοίγει το ίδιο modal με πριν: ημερομηνία, όνομα πελάτη, αριθμός αίτησης, subCategory, status. Οι πίτες είναι **κρυμμένες από προεπιλογή** — κουμπί "▼ Εμφάνιση" δίπλα στον τίτλο "Σε εκκρεμότητα" τις εμφανίζει/αποκρύπτει (`showPendingPies`), μαζί με το date-filter input. Ισχύει για:

- Home: status `ΥΠΟ ΥΛΟΠΟΙΗΣΗ`
- Mobile: status `ΠΡΟΕΓΚΡΙΣΗ`

> Το `ΥΠΟ ΥΛΟΠΟΙΗΣΗ` **δεν** μετράει ως συνδεδεμένο σε καμία κατηγορία — με τη μοναδική εξαίρεση της FTTH στο παράθυρο ② (Vodafone Home), όπου μετράει σαν "pipeline" πίστωση αν καταχωρήθηκε τον ίδιο μήνα (βλ. παραπάνω).

### Home — πριν το Υπό Υλοποίηση (status breakdown)

Μικρό panel ακριβώς κάτω από τα δύο παράθυρα Vodafone Home· μετράει τις εγγραφές Home του "Εκκρεμείς / Δικαιολογητικά" panel (ΛΑΘΟΣ, ΕΛΛΙΠΗ/ΕΚΚΡΕΜΗ ΔΙΚΑΙΟΛΟΓΗΤΙΚΑ, ΚΑΤΑΧΩΡΗΜΕΝΗ, ΝΕΑ) και τις ομαδοποιεί ανά ακριβές status με πλήθος — μια γρήγορη εικόνα του πόσες αιτήσεις "περιμένουν" να φτάσουν σε Υπό Υλοποίηση.

### Home — Υπό Υλοποίηση από προηγούμενους μήνες

Μικρό panel ακριβώς κάτω από το παραπάνω· παίρνει τις Home εγγραφές με status `ΥΠΟ ΥΛΟΠΟΙΗΣΗ` (`homePending`) που **καταχωρήθηκαν σε μήνα διαφορετικό από τον επιλεγμένο**, τις ομαδοποιεί ανά μήνα καταχώρησης (`Ημ/νια Δημιουργίας Αίτησης`) και δείχνει πλήθος + όνομα μήνα ανά ομάδα — ώστε να φαίνεται ποιες εκκρεμότητες "κουβαλιούνται" από παλιότερους μήνες. Δεν εμφανίζεται αν δεν υπάρχουν τέτοιες εγγραφές.

---

### Κατάταξη — ανά κατάστημα, ανά προϊόν/πωλητή (πίτες)

Στο κάτω μέρος της Μηνιαίας προβολής, κάθε ενεργό κατάστημα έχει ένα panel με δύο πίτες ανά κατηγορία (Mobile / Vodafone Home): **"Ανά προϊόν"** (ομαδοποίηση με το ίδιο label grouping του `classifyMobileSubcat`/`formatHomeProductLabel`) και **"Ανά πωλητή"**. Αντικατέστησε τον παλιό πίνακα προϊόν×πωλητής. Όπως και η **Κατάταξη**, αυτά τα panels είναι **κρυμμένα από προεπιλογή** — κουμπί "▼ Εμφάνιση ανάλυσης καταστημάτων" δίπλα στο κουμπί της κατάταξης τα εμφανίζει/αποκρύπτει (`showStorePies`).

- **"Ανά προϊόν"**: κλικ σε φέτα/γραμμή ανοίγει απευθείας τη λίστα αιτήσεων (ημερομηνία, πελάτης, αριθμός αίτησης, status).
- **"Ανά πωλητή"**: κλικ σε πωλητή ανοίγει πρώτα ενδιάμεσο modal με breakdown **του δικού του** συνόλου ανά προϊόν (πόσα κομμάτια ανά υποκατηγορία)· κλικ σε μια υποκατηγορία εκεί μέσα ανοίγει μετά την ίδια λίστα αιτήσεων.

## Γενικοί κανόνες

- Εγγραφές με status `ΑΚΥΡΩΜΕΝΗ` αφαιρούνται πάντα κατά το parsing.
- Εγγραφές με `TRANSFER` στο subcategory αφαιρούνται πάντα κατά το parsing.
- Ο χρήστης αναγνωρίζεται βάσει του ονόματος του αρχείου (με δυνατότητα mapping μέσω Users tab).
- Η εξαίρεση χρήστη (× στα chips) αφαιρεί τον χρήστη από **όλα** τα σύνολα χωρίς να χάνονται τα δεδομένα.
- Παντού εκτός από την **Ημερήσια** καρτέλα, το `viewEntries` αποκλείει επιπλέον εγγραφές με status που περιέχει `ΑΚΥΡΩ`/`ΕΚΚΡΕΜ`/`ΑΠΟΡΡ`, ή status `ΝΕΑ` ακριβώς. Η **Ημερήσια** χρησιμοποιεί ξεχωριστό `dailyEntries` χωρίς αυτό το φίλτρο — δείχνει κάθε status (π.χ. `ΕΚΚΡΕΜΕΙ ΕΛΕΓΧΟΣ`), κρατώντας μόνο τα φίλτρα καταστήματος/χρήστη/dealer code.
- Τα χρώματα Mobile/Vodafone Home είναι επιλέξιμα από τον χρήστη (`categoryColors` state, κουμπιά χρώματος στο dashboard header) και αποθηκεύονται στο `localStorage` (`ws_manager_category_colors`) — προεπιλογή `DEFAULT_CATEGORY_COLORS`. Το Prepay παραμένει στο default χρώμα του (δεν έχει δικό του επιλογέα).
