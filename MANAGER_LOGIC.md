# Manager — Λογική Υπολογισμού ανά Κατηγορία

---

## Αναγνώριση αρχείου

Το σύστημα ανιχνεύει αυτόματα την κατηγορία κάθε Excel βάσει των headers:

| Header που βρίσκει   | Κατηγορία      |
| -------------------- | -------------- |
| `Ημ/νία Αίτησης`     | Mobile         |
| `MSISDN`             | Prepay         |
| `Τηλέφωνο Υπηρεσίας` | Vodafone Home  |

---

## Mobile

### Ποιες εγγραφές μετράνε

Μόνο τα παρακάτω subcategories (`Τύπος Αίτησης`) υπολογίζονται:

- EX PREPAY, PRE2EC, PORT IN POSTPAY, PORT IN EC, NEW CONNECTION, NEW EC, PREPAY 2 EC

Εξαιρούνται πάντα: εγγραφές με `TRANSFER` στον τύπο, και εγγραφές με status `ΑΚΥΡΩΜΕΝΗ`.

### Καταχωρημένες μήνα

Χρησιμοποιεί: **`Ημ/νία Αίτησης`** (date)

> Η αίτηση ανήκει στον μήνα που υποβλήθηκε.

### Συνδεδεμένες μήνα

Χρησιμοποιεί: **`Ημερομηνία Έγκρισης`** (implDate) — αν είναι κενή, fallback στο `Ημ/νία Αίτησης`  
Status απαιτείται: **`ΟΛΟΚΛΗΡΩΘΗΚΕ`**

### Ειδική περίπτωση: PORT IN PREPAY

Εγγραφές mobile με subcategory `PORT IN PREPAY` μετράνε στη στήλη **Prepay** (όχι Mobile).  
Χρησιμοποιεί implDate ή date, status `ΟΛΟΚΛΗΡΩΘΗΚΕ`.

### Πεδίο Connections

Αν το πεδίο `Αριθμός Συνδέσεων` > 1, χρησιμοποιείται ως πολλαπλασιαστής στο σύνολο.

---

## Prepay

### Καταχωρημένες μήνα

Χρησιμοποιεί: **`Ημερομηνία Δημιουργίας`**

### Συνδεδεμένες μήνα

Χρησιμοποιεί: **`Ημερομηνία Ολοκλήρωσης`** — αν κενή, fallback στο `Ημερομηνία Δημιουργίας`  
Status απαιτείται: **`ΟΛΟΚΛΗΡΩΘΗΚΕ`**

---

## Vodafone Home

### Ποιες εγγραφές μετράνε

Όλες εκτός από εγγραφές με `TRANSFER` στο subcategory (`Προγραμμά Χρήσης · Ταχύτητα`).

### Ταξινόμηση προϊόντος (`classifyHomeProduct`)

Κάθε γραμμή Home ταξινομείται σε έναν από τέσσερις τύπους, με βάση το `subCategory` (= `Προγραμμά Χρήσης · Ταχύτητα`):

| Τύπος     | Κανόνας ανίχνευσης                                            |
| --------- | -------------------------------------------------------------- |
| One Net   | Ξεχωριστή γραμμή από το αρχείο **Mobile** με `Τύπος Αίτησης` που περιέχει `FIXED ACTIVATION` — αναταξινομείται αυτόματα σε Home |
| Wireless  | `Προγραμμά Χρήσης` περιέχει `Wireless`                          |
| FTTH      | `Ταχύτητα` περιέχει `FTTH`                                      |
| FTTC      | οτιδήποτε άλλο (fallback)                                       |

### Καταχωρημένες μήνα

Χρησιμοποιεί: **`Ημ/νια Δημιουργίας Αίτησης`**

### Δύο παράθυρα μηνιαίας ανάλυσης

**① Συνδεδεμένα Μήνα** — όλες οι γραμμές Home που συνδέθηκαν (Κ5) τον μήνα, **ανεξαρτήτως πότε καταχωρήθηκαν**, με ανάλυση ανά τύπο (FTTH / FTTC / Wireless / One Net).

- Χρησιμοποιεί: **`Ημ/νια Ολοκλήρωσης (Κ5)`** — **αυστηρά, χωρίς fallback**. Αν δεν υπάρχει Κ5 ημερομηνία, η εγγραφή δεν μετράει ως συνδεδεμένη.
- Status απαιτείται: `ΟΛΟΚΛΗΡΩΘΗΚΕ` ή `ΥΛΟΠΟΙΗΜΕΝΗ`

**② Μετράνε στον Μήνα** (επίσημο KPI/στόχος) — διαφορετικός κανόνας ανά τύπο:

- **FTTC / Wireless / One Net**: μετράνε όλα τα συνδεδεμένα του μήνα (ίδιο σύνολο με το παράθυρο ①), ανεξαρτήτως πότε καταχωρήθηκαν.
- **FTTH**: μετράει **μόνο** αν συνδέθηκε (Κ5) τον μήνα **ΚΑΙ** καταχωρήθηκε (`Ημ/νια Δημιουργίας Αίτησης`) τον **ίδιο** μήνα.
- **FTTH σε εκκρεμότητα**: επιπλέον μετράνε όσες FTTH καταχωρήθηκαν τον μήνα και έχουν status `ΥΠΟ ΥΛΟΠΟΙΗΣΗ` (δεν έχουν συνδεθεί ακόμα — "πίστωση" για το ότι μπήκαν στο pipeline μέσα στον μήνα).

> Μια FTTH που καταχωρήθηκε π.χ. τον Ιούλιο αλλά συνδέθηκε τον Αύγουστο **δεν** μετράει στο ② του Αυγούστου (ούτε στο ② του Ιουλίου, αφού δεν είχε συνδεθεί ακόμα) — εμφανίζεται μόνο στο παράθυρο ① του Αυγούστου ως ενημερωτικό στοιχείο.

---

## Panels πλάγιου πληροφόρησης

### Εκκρεμείς / Δικαιολογητικά

Εμφανίζει **μεμονωμένες αιτήσεις** (με customer, requestId, status) για:

| Κατηγορία | Status που εμφανίζεται                                                  |
| --------- | ----------------------------------------------------------------------- |
| Home      | ΛΑΘΟΣ, ΕΛΛΙΠΗ ΔΙΚΑΙΟΛΟΓΗΤΙΚΑ, ΕΚΚΡΕΜΗ ΔΙΚΑΙΟΛΟΓΗΤΙΚΑ, ΚΑΤΑΧΩΡΗΜΕΝΗ, ΝΕΑ |
| Mobile    | ΝΕΑ, ΚΑΤΑΧΩΡΗΜΕΝΗ                                                       |

### Υπό Υλοποίηση

Εμφανίζει **λίστα ανά πωλητή** — αριθμός αιτήσεων + ονόματα πελατών από κάτω από κάθε πωλητή — για:

- Home: status `ΥΠΟ ΥΛΟΠΟΙΗΣΗ`
- Mobile: status `ΠΡΟΕΓΚΡΙΣΗ`

> Το `ΥΠΟ ΥΛΟΠΟΙΗΣΗ` **δεν** μετράει ως συνδεδεμένο σε καμία κατηγορία — με τη μοναδική εξαίρεση της FTTH στο παράθυρο ② (Vodafone Home), όπου μετράει σαν "pipeline" πίστωση αν καταχωρήθηκε τον ίδιο μήνα (βλ. παραπάνω).

### Home — πριν το Υπό Υλοποίηση (status breakdown)

Μικρό panel ακριβώς κάτω από τα δύο παράθυρα Vodafone Home· μετράει τις εγγραφές Home του "Εκκρεμείς / Δικαιολογητικά" panel (ΛΑΘΟΣ, ΕΛΛΙΠΗ/ΕΚΚΡΕΜΗ ΔΙΚΑΙΟΛΟΓΗΤΙΚΑ, ΚΑΤΑΧΩΡΗΜΕΝΗ, ΝΕΑ) και τις ομαδοποιεί ανά ακριβές status με πλήθος — μια γρήγορη εικόνα του πόσες αιτήσεις "περιμένουν" να φτάσουν σε Υπό Υλοποίηση.

---

## Γενικοί κανόνες

- Εγγραφές με status `ΑΚΥΡΩΜΕΝΗ` αφαιρούνται πάντα κατά το parsing.
- Εγγραφές με `TRANSFER` στο subcategory αφαιρούνται πάντα κατά το parsing.
- Ο χρήστης αναγνωρίζεται βάσει του ονόματος του αρχείου (με δυνατότητα mapping μέσω Users tab).
- Η εξαίρεση χρήστη (× στα chips) αφαιρεί τον χρήστη από **όλα** τα σύνολα χωρίς να χάνονται τα δεδομένα.
